# 夜班 Review 报告 — 2026-09-05

## 1. 执行时间
2026-09-05 凌晨（夜间无人值守）

## 2. Git 分支
`22`

## 3. Git 状态（开工时）
- 工作区干净（无已跟踪文件修改）
- 未跟踪文件 2 个（用户所有，全程未触碰）：`AGENTS.md`、`e2e/verify-right-panel.mjs`

## 4. 最近提交
HEAD = `51433652`。最近 5 次提交全部围绕"量测测点查重/还原/告警"（13bb555b → 51433652），改动集中在：
- `src/appExtracted/appGraphMeasurementFactories.tsx`（+66/-9）
- `src/appGraphMeasurementFactories.test.ts`（+95）

## 5. 工作区是否存在用户修改
存在 2 个用户未跟踪文件，已保护未动。无其他用户修改。

## 6. 检查范围
1. 最近 5 次提交的完整 diff 审查（量测查重还原逻辑、保存合规校验、【默认】按钮、message.config）
2. 查重覆盖面追踪：`createUpdateMeasurementItem` 全部调用方、量测编辑弹窗确认链路、保存合规校验调用点
3. `measurementSourcePointForNodeItem` 与还原/按钮两处默认测点公式比对
4. 全量测试基线（vitest：2279 用例）
5. HEAD~5 历史基线对比（独立 worktree）判断失败是否新引入
6. 全库 TODO/FIXME 扫描（0 处）

## 7-8. 发现的问题与证据

### NS-001（P1）：还原"默认测点"公式与创建时规范公式不一致
- 还原分支（`appGraphMeasurementFactories.tsx:4583`）与【默认】按钮（`:5117-5119`）默认测点 = `节点ID[.端子ID].量测类型ID`；创建时规范公式（`:4439-4440`）= `associatedField || (role?.)+"量测类型ID"`。
- **已用临时可执行测试复现证实**：带 `associatedField: "Ua"` 的项原测点 `node-a.t1.Ua`，被重复拒绝后还原为 `node-a.t1.activePower` —— 原绑定被覆盖；且同组同类型两项时还原值自身可能重复。
- 详细记录：`suggestions/2026-09-05.md`、`tasks/TASKS.md#NS-001`

### NS-002（P2）：量测编辑弹窗确认只查重名称，不查重测点（`:4822-4826`），与右侧面板/保存校验策略不一致。

### NS-003（P2）：全量测试 30 failed / 2249 passed，**均为历史遗留**
- 证据：HEAD~5 worktree 上 4 个量测测试失败同样存在。
- 典型根因：测试与实现漂移（padding 空格期望、`setLastCanvasClickTarget` scope 缺函数）、环境数据依赖（`ems-rtdb-backend-restore` 读本地 `data/`）。

### NS-004（P3 备忘）：`message.config({ top: 50 })` 为模块顶层全局副作用，影响全应用 message 位置（51433652 有意行为）。

## 9. 风险等级
- NS-001：P1（确定的行为缺陷，修复方向需产品决策）
- NS-002：P2（一致性缺口，低概率触发）
- NS-003：P2（掩盖回归信号）
- NS-004：P3（备忘）

## 10. 是否允许自动修改
- NS-001：否 —— 权威默认公式属产品决策，且按钮文案表明现有行为可能有意；无法证明 100% 无副作用。
- NS-002：否 —— 属功能补全（新增校验行为）。
- NS-003：否 —— 改测试预期/删测试被禁止；补 scope 属行为新增。
- NS-004：否 —— 有意行为。

## 11. 实际修改内容
**未修改任何业务代码。** 非代码操作：
1. 初始化 `reviewer/` 记录目录；
2. 临时复现测试（证实 NS-001 后已删除，未入库）；
3. 环境事故修复：worktree junction 清理误删 `node_modules`（仅依赖目录；源码/用户文件/git 历史经 `git status` 核对完好），已 `pnpm install` 完整恢复并验证基线一致。详见 `completed/2026-09-05.md`。

## 12. 测试结果
- 全量：30 failed / 2249 passed（失败均为历史遗留，非最近提交引入）
- `appGraphMeasurementFactories.test.ts`：4 failed / 35 passed（HEAD 与 HEAD~5 相同 → 历史遗留）
- 新增查重相关单测（57b0e87f 补充的用例）全部通过

## 13. 未处理任务
- NS-001（P1，待人工决策）、NS-002（P2）、NS-003（P2）→ `tasks/TASKS.md`

## 14. 下一步建议
1. **优先决策 NS-001**：确认权威默认测点公式；若以创建公式为准，修复点为 `:4583` 与【默认】按钮，并补单测（含"还原值自身不得重复"校验）。
2. NS-002：弹窗确认补测点查重，与名称查重并列。
3. NS-003：安排一次"测试漂移清理"专项：逐个判定测试与实现孰对（尤其 `swigger.examples` 10 例依赖运行环境/数据，建议标注或改为需要本地服务时跳过）。
4. 过程教训：Windows 上对含 junction 的目录禁用 `--force` 递归删除。
